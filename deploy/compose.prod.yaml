# Production stack for the demo server.
# Caddy (outside this stack, on the external "web" network) terminates TLS and
# routes DRIVE_DOMAIN -> nginx, AUTH_DOMAIN -> keycloak, S3_DOMAIN -> minio.
# Variables come from deploy/.env, generated once by deploy/setup.sh.
name: drive-prod

x-backend: &backend
  image: drive-prod:backend
  restart: unless-stopped
  environment:
    # The image user has no home: gunicorn's control socket needs a writable one.
    HOME: /tmp
  env_file:
    - env/backend.env
  depends_on:
    postgresql:
      condition: service_healthy
    redis:
      condition: service_started
    minio:
      condition: service_healthy

services:
  postgresql:
    # Postgres 16 with the pgvector extension (file graph embeddings); the
    # trixie variant keeps the glibc collation of the previous postgres:16.
    image: pgvector/pgvector:0.8.6-pg16-trixie
    restart: unless-stopped
    env_file:
      - env/postgresql.env
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -d $${POSTGRES_DB} -U $${POSTGRES_USER}"]
      interval: 2s
      timeout: 2s
      retries: 60
    volumes:
      - postgresql:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    restart: unless-stopped

  tika:
    # Text extraction (with OCR) for the file graph; reached by celery as
    # http://tika:9998 (GRAPH_TIKA_URL). The image ships no curl or wget, so
    # no healthcheck: Tika answers within seconds of starting.
    image: apache/tika:3.3.1.0-full
    restart: unless-stopped

  minio:
    image: quay.io/minio/minio
    container_name: drive-prod-minio
    restart: unless-stopped
    env_file:
      - env/minio.env
    command: server --console-address :9001 /data
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 2s
      timeout: 20s
      retries: 60
    volumes:
      - media:/data
    networks:
      - default
      - web

  createbuckets:
    image: quay.io/minio/mc
    env_file:
      - env/minio.env
    depends_on:
      minio:
        condition: service_healthy
    entrypoint: >
      sh -c "
      mc alias set drive http://minio:9000 $${MINIO_ROOT_USER} $${MINIO_ROOT_PASSWORD} &&
      mc mb --ignore-existing drive/drive-media-storage &&
      mc version enable drive/drive-media-storage"

  backend:
    <<: *backend
    build:
      context: ..
      target: backend-production
      args:
        DOCKER_USER: "1000:1000"

  migrate:
    <<: *backend
    restart: "no"
    command: ["python", "manage.py", "migrate", "--noinput"]

  celery:
    <<: *backend
    # -B embeds beat: it refreshes the WOPI discovery cache periodically.
    command: ["celery", "-A", "drive.celery_app", "worker", "-B", "-l", "INFO",
              "--schedule", "/tmp/celerybeat-schedule"]

  onlyoffice:
    image: onlyoffice/documentserver-de
    container_name: drive-prod-onlyoffice
    restart: unless-stopped
    env_file:
      - env/onlyoffice.env
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/hosting/discovery"]
      interval: 30s
      retries: 10
      start_period: 120s
      timeout: 10s
    volumes:
      - ./onlyoffice/local-production-linux.json:/etc/onlyoffice/documentserver/local-production-linux.json:ro
    networks:
      - default
      - web

  frontend:
    image: drive-prod:frontend
    restart: unless-stopped
    build:
      context: ..
      dockerfile: ./src/frontend/Dockerfile
      target: frontend-production
      args:
        API_ORIGIN: "https://${DRIVE_DOMAIN}"
        DOCKER_USER: "1000:1000"

  nginx:
    image: nginx:1.27-alpine
    container_name: drive-prod-nginx
    restart: unless-stopped
    volumes:
      # Mount the directory, not the file: git replaces the file on each deploy
      # and a single-file bind mount would keep serving the old copy.
      - ./nginx:/etc/nginx/conf.d:ro
    depends_on:
      - backend
      - frontend
    networks:
      - default
      - web

  kc_postgresql:
    image: postgres:16
    restart: unless-stopped
    env_file:
      - env/kc_postgresql.env
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -d $${POSTGRES_DB} -U $${POSTGRES_USER}"]
      interval: 2s
      timeout: 2s
      retries: 60
    volumes:
      - kc_postgresql:/var/lib/postgresql/data

  keycloak:
    image: quay.io/keycloak/keycloak:26.3.2
    container_name: drive-prod-keycloak
    restart: unless-stopped
    volumes:
      - ./keycloak/realm.json:/opt/keycloak/data/import/realm.json:ro
    command:
      - start-dev
      - --import-realm
      - --hostname=https://${AUTH_DOMAIN}
      - --proxy-headers=xforwarded
      - --http-enabled=true
      - --health-enabled=true
    env_file:
      - env/keycloak.env
    depends_on:
      kc_postgresql:
        condition: service_healthy
    networks:
      - default
      - web

volumes:
  postgresql:
  kc_postgresql:
  media:

networks:
  web:
    external: true
